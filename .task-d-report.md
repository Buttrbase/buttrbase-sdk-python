# Task D Report — token-claims-enrichment

## Status

DONE

## Commit

`cbac970` on `feat/token-claims-enrichment`

## Test summary

164 passed, 0 failed (17 new tests added; all 147 pre-existing tests unchanged)

## What was done

1. **New module** `src/buttrbase/verify.py` — pure-stdlib, no new deps:
   - `ClaimsData` dataclass: `roles`, `email`, `org_uuid`, `user_uuid` from the `data` envelope
   - `Claims` dataclass: `sub`, `org`, `exp`, `iat`, `scope`, `data`; `from_dict(payload)` parses a decoded JWT payload dict
   - `TokenPrincipal` dataclass: Python equivalent of Rust's `AuthContext`; `from_claims()` splits `data.roles` on `[,\s]+` (matching Rust `split([',', ' ']).filter(!empty)`) into `roles: list[str]` and lifts `email: Optional[str]`
   - `principal_from_payload(payload)` one-shot helper

2. **`__init__.py`** — re-exports all four names + `verify` module subpackage; `__version__` bumped `0.3.1 → 0.5.0`

3. **`pyproject.toml`** — version bumped `0.4.0 → 0.5.0`

4. **`CHANGELOG.md`** — `## 0.5.0` section added mirroring Rust SDK 0.6.0 entry

5. **`README.md`** — "Token Claims Enrichment" section added with usage example

6. **`tests/test_unit.py`** — three new test classes (`TestClaimsDataEnrichment`, `TestClaimsFromDict`, `TestTokenPrincipal`); key fixture test `test_fixture_principal_roles_and_email` asserts `principal_from_payload(fixture)` yields `roles == ["owner"]` and `email == "test@example.com"` using the shared `buttrbase-sdk-rust/tests/fixtures/access_token_claims.json`; inline fallback keeps tests green in checkouts without the Rust sibling

## Concerns

None. The SDK had no existing verify module — this is a clean addition. No existing symbols were modified or removed. No new runtime dependencies introduced (pure stdlib dataclasses + re).
